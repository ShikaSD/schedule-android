apply plugin: 'com.parse'

buildscript {
    repositories {
        jcenter()
        mavenCentral()
        maven {
            url 'https://maven.parse.com/repo'
        }
        maven { url 'https://maven.fabric.io/public' }
    }
    dependencies {
        classpath 'com.parse.tools:gradle:1.+'
        classpath 'com.android.tools.build:gradle:1.1.1'
        classpath 'io.fabric.tools:gradle:1.+'
    }
}
apply plugin: 'com.android.application'
apply plugin: 'io.fabric'

repositories {
    jcenter()
    maven { url 'https://maven.fabric.io/public' }
}

parse {
    applicationId "eR4X3CWg0H0dQiykPaWPymOLuceIj7XlCWu3SLLi"
    masterKey "AafJe4bHembFbmp1GxJzobtsLTNQE4u3SXAhgDB0"
    uploadSymbols true
    retries 3
}

android {
    compileSdkVersion 21
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "ru.shika.mamkschedule"
        minSdkVersion 10
        targetSdkVersion 21
        versionCode 0
        versionName "0.5"
        buildTypes {
            release {
                proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            }
        }
    }
    signingConfigs {
        release {
            storeFile file('C:/Ghjuf/Android/keystore/keystore.jks')
            storePassword "Oo3WcEf"
            keyAlias "Shika"
            keyPassword "Oo3WcEf"
        }
    }
    buildTypes {
        release {
            debuggable true
            signingConfig signingConfigs.release
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:support-v4:22.+'
    compile 'com.android.support:appcompat-v7:22.+'
    compile 'com.android.support:cardview-v7:22.+'
    compile 'com.android.support:recyclerview-v7:22.+'
    compile 'com.parse.bolts:bolts-android:1.+'
    compile('com.crashlytics.sdk.android:crashlytics:2.5.0@aar') {
        transitive = true;
    }
}
